import { U as User } from './types-j5ABA33q.js';
import '@coinbase/wallet-sdk';
import '@simplewebauthn/browser';
import '@solana/kit';
import 'react';
import 'viem';
import '@privy-io/api-types';
import '@privy-io/js-sdk-core';
import '@base-org/account';
import '@wallet-standard/base';
import '@solana/wallet-standard-features';
import '@wallet-standard/features';
import 'eventemitter3';

type UseLoginWithSsoToken = {
    /**
     * Authenticates a user by exchanging an SSO token for a session.
     *
     * This hook implements a secure code exchange flow where the token
     * is exchanged server-side for a session response. This prevents exposing
     * access tokens in URLs.
     *
     * @returns Promise resolving to the authenticated User
     * @example
     * ```typescript
     * const {loginWithSsoToken} = useLoginWithSsoToken();
     *
     * // In your SSO callback page:
     * await loginWithSsoToken();
     * ```
     *
     * @experimental
     */
    loginWithSsoToken: () => Promise<User>;
};
/**
 *
 * Hook to authenticate a user using an SSO token.
 *
 * @experimental
 */
declare function useLoginWithSsoToken(): UseLoginWithSsoToken;

type DepositAddressFlowParams = {
    /** CAIP-2 chain identifier for the destination (e.g. eip155:1, solana:5eykt4UsFv8P8NJdTREpY1vzqKqZKvdp). */
    destinationChain: string;
    /** Token symbol for the destination asset (e.g. usdc). */
    destinationCurrency: string;
    /** Wallet address to receive the deposited funds. */
    destinationAddress: string;
    /** Refund address on the source chain. Resolved by the modal if not provided. */
    refundAddress?: string;
    slippageBps?: number;
};

type UseDepositAddressResult = {
    /** Opens the source selection modal. Resolves on success, rejects on error. */
    createDepositAddress: (opts: DepositAddressFlowParams) => Promise<void>;
};
/** @experimental This interface may change at any time. */
declare const useDepositAddress: () => UseDepositAddressResult;

export { useDepositAddress, useLoginWithSsoToken };
export type { UseDepositAddressResult, UseLoginWithSsoToken };
